import React from 'react';
import Header from '../Shared/Header/Header';

const Blog = () => {
    return (
        <div className="section">
            <Header></Header>
        </div>
    );
};

export default Blog;